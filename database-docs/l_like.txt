create table l_like
(
id serial,
u_id bigint(20) unsigned not null,
mb_id bigint(20) unsigned not null,

primary key (id),
foreign key (id) references user(id),
foreign key (id) references moodboard(id)
)
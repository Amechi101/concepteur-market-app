create table l_comment
(
id serial,
u_id bigint(20) unsigned not null,
l_id bigint(20) unsigned not null,

timestamp datetime,
text text,

primary key (id),
foreign key (id) references user(id),
foreign key (id) references loo(id)
)
create table rewardpool
(
uid bigint(20) unsigned not null,
rid bigint(20) unsigned not null,
isvalid boolean,
expire date,

primary key (uid, rid),
foreign key (uid) references user(id),
foreign key (rid) references reward(id)
)
create table user
(
id serial,

email varchar(30) unique,
password varchar(30) not null,
fname varchar(30),
lname varchar(30),
gender tinyint,
birthday date,

current_points int,
lifetime_points int,

primary key (id)
)
create table d_comment
(
id serial,
u_id bigint(20) unsigned not null,
mb_id bigint(20) unsigned not null,

timestamp datetime,
text text,

primary key (id),
foreign key (u_id) references user(id),
foreign key (mb_id) references moodboard(id)
)

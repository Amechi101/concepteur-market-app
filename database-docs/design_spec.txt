create table design_spec
(
id serial,
c_id bigint(20) unsigned not null,

color_name varchar(20),
size_name varchar(5),
measurements varchar(255),
quantity int,
price decimal(9,2),

primary key (id),
foreign key (cid) references clothing(id)
)
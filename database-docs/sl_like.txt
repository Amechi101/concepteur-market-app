create table sl_like
(
id serial,
u_id bigint(20) unsigned not null,
sl_id bigint(20) unsigned not null,

primary key (id),
foreign key (id) references user(id),
foreign key (id) references saved_look(id)
)
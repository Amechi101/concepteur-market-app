create table address
(
id serial,
u_id bigint(20) unsigned not null,

address varchar(50),
city varchar(50),
state varchar(2)
zipcode int,
is_billing boolean,
country varchar(20),

primary key (id),
foreign key (u_id) references user(id)
)
